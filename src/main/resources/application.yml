server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/defect_manager?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
